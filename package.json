{
  "name": "slimebot-workspace",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"go run ./cmd/server/main.go server\" \"npm --prefix frontend run dev\"",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm --prefix frontend run build && npm --prefix cli run build && go build -o slimebot ./cmd/server",
    "start": "go run ./cmd/server/main.go server",
    "build:cli": "npm --prefix cli run build",
    "cli": "npm --prefix cli run build && go run ./cmd/server cli",
    "package": "scripts/package-release.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
